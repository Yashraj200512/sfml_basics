// library //in game directory
#include <iostream>
#include <SFML/Graphics.hpp>

// main program

int main()
{ // create window
    std::cout << "Hello World!" << std::endl;
    sf::RenderWindow window(sf::VideoMode({800, 600}), "Title");

    // program end successfully
    return 0;
}
